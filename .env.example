# AI Assistant Competition - environment variable template
# Copy to a local non-committed file (for example .env.local) and fill values.
# Never commit real secrets.

# App runtime DB URL (what the Streamlit app uses)
DATABASE_URL=postgresql://username:password@host:5432/postgres

# Makefile DB targets
PRODUCTION_DATABASE_URL=postgresql://username:password@host:5432/postgres
STAGING_DATABASE_URL=postgresql://username:password@host:5432/postgres
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD_HASH=

# Encryption key for user API key storage (Fernet base64 key)
API_KEY_ENCRYPTION_KEY=

# Optional E2E settings
E2E_INSTRUCTOR_EMAIL=
E2E_INSTRUCTOR_PASSWORD=
E2E_BASE_URL=http://localhost:8501
E2E_OPENAI_API_KEY=
